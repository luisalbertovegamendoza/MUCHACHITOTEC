services:
  - type: web
    name: muchachitotec
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: gunicorn JJTECNOLOGIAEIRL.wsgi:application
